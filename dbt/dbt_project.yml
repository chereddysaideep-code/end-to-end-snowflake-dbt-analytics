name: 'end_to_end_analytics'
version: '1.0.0'
config-version: 2

profile: 'end_to_end_analytics'

model-paths: ["models"]
test-paths: ["tests"]

models:
  end_to_end_analytics:

    staging:
      +materialized: view

    intermediate:
      +materialized: view

    marts:
      +materialized: table
